[pytest]
# 测试文件发现规则
testpaths = testcases
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# Allure 报告输出目录
addopts =
    --alluredir=reports/allure-results
    -v
    --tb=short

# 自定义标记
markers =
    ui: UI 自动化测试
    api: 接口测试
    slow: 耗时较长的测试
